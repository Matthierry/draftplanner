export type Position='GK'|'DEF'|'MID'|'FWD';
export type StrategyMode='Safe'|'Balanced'|'Upside';
export type MarketStatus='Not Started'|'Very Overheated'|'Overheated'|'Fair'|'Soft'|'Very Soft'|string;
export type PositionSettings=Record<Position,{minActive:number;maxActive:number;draftMinimum:number;maxSquad:number}>;
export type Settings={season:string;managerCount:number;startingBudget:number;squadSize:number;activePlayers:number;benchPlayers:number;riskMode:StrategyMode;starterPointActive:string;userManagerName:string;firstTenPercentTarget:number;totalDraftPicks:number;positions:PositionSettings};
export type Manager={id:string;name:string;active:boolean;startingBudget:number;remainingBudget:number;playersDrafted:number;squadSpotsLeft:number;maxLegalBid:number;drafted:Record<Position,number>;needs:Record<Position,number>;needScore:number;dangerLevel:string};
export type Player={id:string;name:string;team:string;eligiblePos:string;lockedPos:string;tier:string;preference:number;reliability:number;avoidUnlessCheap:string;transferRisk:string;manualReview:string;points:number;fpg:number;siteProjection:number;yourProjection:number;finalProjection:number;replacementLevel:number;pointsAboveReplacement:number;baseDollarValue:number;tierMultiplier:number;preferenceMultiplier:number;reliabilityMultiplier:number;transferRiskMultiplier:number;avoidMultiplier:number;adjustedDollarValue:number;marketDollarValue:number;status:string;winningManager:string;soldPrice:number;openingBid:number;goodPrice:number;fairPrice:number;stretchPrice:number;hardMax:number;liveMaxBid:number;nominationType:string;nominationScore:number;notes:string};
export type AuctionSale={rowNumber:number;pick:number;playerId:string;playerName:string;position:Position;team:string;soldPrice:number;winningManager:string;expectedValue:number;vsValue:number;marketStatus:string;notes:string};
export type Dashboard={currentPick:number;totalPicks:number;draftCompletion:number;userRemainingBudget:number;userMaxLegalBid:number;marketSpendIndex:number;marketStatus:MarketStatus;firstTenWindow:number;userPlayersDrafted:number;firstTenAlert:string};
export type DraftState={settings:Settings;managers:Manager[];players:Player[];auctionLog:AuctionSale[];dashboard:Dashboard;lastSync:string};
